# Тестовое задание

## Как поднять

1) Открыть папку с заданием в терминале
2) Ввести `docker compose up --build` при первом запуске
3) Сервис запущен
4) При последующих запусках ввести `docker compose up`
